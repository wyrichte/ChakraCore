var e = Debug.createTypedObject(2000, "mytype", 32, true);
var f = Debug.createTypedObject(2000, "mytype", 32, true);
function installPrototype() {
  e.__proto__ = {blah: 1};
}

var obj = new Object();
function installObjPrototype() {
  obj.__proto__ = {bar: 1};
}

var typedArray = new Int8Array(9);
typedArray.foo = 20;
function installTypedArrayPrototype() {
    e.__proto__ = typedArray;
}

function installTypedArrayPrototype2(typedArray2) {
    e.__proto__ = typedArray2;
    // Tests that ArrayBuffer is marshalled correctly along with its prototype chain.
    // Accessing the buffer.constructor below causes the ArrayBuffer to be marshalled.
    e.ConstructorName = e.__proto__.buffer.constructor.name;
}
