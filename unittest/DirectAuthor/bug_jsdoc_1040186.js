Object.prototype.myobj = {};
var hash = null;
/**
* param key
* @returns {String} the value for the key
*/
function foo() {
    
}

function bar() {
    if (!foo()) {
        hash = new Map();
    }
}
foo()./**ml:concat**/;
bar();
hash./**ml:set,entries**/;