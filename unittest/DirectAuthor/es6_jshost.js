/**ref:..\\..\\Lib\\Author\\References\\libhelp.js**/

Promise./**ml:all,race,reject,resolve**/;
Promise(/**pl:***/);
Promise.all(/**pl:***/);
Promise.race(/**pl:***/);
Promise.reject(/**pl:***/);
Promise.resolve(/**pl:***/);

var p = new Promise(function() {});
p./**ml:catch,then**/;
p.catch(/**pl:***/);
p.then(/**pl:***/);

Symbol./**ml:hasInstance,isConcatSpreadable,iterator,toPrimitive,toStringTag,unscopables,for,keyFor**/;
Symbol.for(/**pl:***/);
Symbol.keyFor(/**pl:***/);

ArrayBuffer(/**pl:***/);
ArrayBuffer./**ml:isView**/;
ArrayBuffer.isView(/**pl:***/);
var a = new ArrayBuffer(0);
a./**ml:slice,byteLength**/;
a.slice(/**pl:***/);

DataView(/**pl:***/);
var d = new DataView(a);
d./**ml:setInt8,setUint8,setInt16,setUint16,setInt32,setUint32,setFloat32,setFloat64,getInt8,getUint8,getInt16,getUint16,getInt32,getUint32,getFloat32,getFloat64,buffer,byteLength,byteOffset**/;
d.getFloat32(/**pl:***/);
d.getFloat64(/**pl:***/);
d.getInt8(/**pl:***/);
d.getUint8(/**pl:***/);
d.getInt16(/**pl:***/);
d.getUint16(/**pl:***/);
d.getInt32(/**pl:***/);
d.getUint32(/**pl:***/);
d.setFloat32(/**pl:***/);
d.setFloat64(/**pl:***/);
d.setInt8(/**pl:***/);
d.setUint8(/**pl:***/);
d.setInt16(/**pl:***/);
d.setUint16(/**pl:***/);
d.setInt32(/**pl:***/);
d.setUint32(/**pl:***/);

WeakSet(/**pl:***/);
var w = new WeakSet();
w./**ml:add,delete,has**/;
w.add(/**pl:***/);
w.delete(/**pl:***/);
w.has(/**pl:***/);

Uint8ClampedArray./**ml:BYTES_PER_ELEMENT**/;
Uint8ClampedArray(/**pl:***/);
Uint8ClampedArray.from(/**pl:***/);
Uint8ClampedArray.of(/**pl:***/);
var u = new Uint8ClampedArray(10);
u./**ml:BYTES_PER_ELEMENT,set,subarray,copyWithin,every,fill,filter,find,findIndex,forEach,indexOf,join,lastIndexOf,map,reduce,reduceRight,reverse,slice,some,sort,buffer,byteLength,byteOffset,length,toLocaleString,toString,entries,keys,values**/;
u.copyWithin(/**pl:***/);
u.entries(/**pl:***/);
u.every(/**pl:***/);
u.fill(/**pl:***/);
u.filter(/**pl:***/);
u.find(/**pl:***/);
u.findIndex(/**pl:***/);
u.forEach(/**pl:***/);
u.indexOf(/**pl:***/);
u.join(/**pl:***/);
u.keys(/**pl:***/);
u.lastIndexOf(/**pl:***/);
u.map(/**pl:***/);
u.reduce(/**pl:***/);
u.reduceRight(/**pl:***/);
u.reverse(/**pl:***/);
u.set(/**pl:***/);
u.slice(/**pl:***/);
u.some(/**pl:***/);
u.sort(/**pl:***/);
u.subarray(/**pl:***/);
u.toString(/**pl:***/);
u.toLocaleString(/**pl:***/);
u.values(/**pl:***/);

Math./**ml:E,LN10,LN2,LOG2E,LOG10E,PI,SQRT1_2,SQRT2,abs,acos,asin,atan,atan2,cos,ceil,exp,floor,log,max,min,pow,random,round,sin,sqrt,tan,imul,fround,log10,log2,log1p,expm1,cosh,sinh,tanh,acosh,asinh,atanh,hypot,trunc,sign,cbrt,clz32**/;

Array(/**pl:***/);
Array./**ml:isArray,from,of**/;
Array.from(/**pl:***/);
Array.of(/**pl:***/);
var a = new Array(10);
a./**ml:push,concat,join,pop,reverse,shift,slice,sort,splice,toLocaleString,toString,unshift,indexOf,every,filter,forEach,lastIndexOf,map,reduce,reduceRight,some,find,findIndex,entries,keys,values,fill,copyWithin**/;
a.copyWithin(/**pl:***/);
a.fill(/**pl:***/);
a.entries(/**pl:***/);
a.keys(/**pl:***/);
a.values(/**pl:***/);

"abc"./**ml:includes**/;

"abc".includes(/**pl:***/"a", 0);
