
/**
 * If I do not specify the type here - let's make sure we work.
 * @type
 */
var emptyFunction = "test";

/**
 * If I do not specify the type here - let's make sure we work.
 * @property
 */
var emptyFunction = "test";

var o = {
   /**
   * @property
   */
   test: "Hello"
};

/**
 * If we provide an unknown jsdoc tag - let's make sure we work.
 * @typeProvider
 */
var emptyFunction = "test";

/**ml:-**/