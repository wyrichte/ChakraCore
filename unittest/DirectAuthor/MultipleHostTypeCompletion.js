var cc;

if (typeof WinRTError !== 'undefined') {
    cc = {application:1};
} else {
    cc = {browser:1};
}

cc.|;

// String methods
var s = 'string';
s.|;

// Date methods
var d = new Date();
d.|;

// Conditional Compilation in context files
context1.|;