/**
 * @param {Array.<String>} here - I am just an array of string.
 */
function array1(here)
{
    return here[0]./**ml:charAt**/charAt(0);
}

/**
 * @param {Array[String]} here - I am just an array of string.
 */
function array2(here)
{
    return here[0]./**ml:charAt**/charAt(0);
}

